# 학습된 모델 가중치 — GitHub 100MB 제한 초과, outputs_ast.zip에서 복원할 것
*.pt
*.pth
*.onnx
*.engine

# 파이썬
__pycache__/
*.pyc

# Claude Code 임시 파일
.claude/*.tmp.*

# Orbbec SDK 실행 시 자동 생성되는 로그
Log/

# Obsidian 에디터 로컬 워크스페이스 상태 (기기별 창 배치 등, 공유 대상 아님)
.obsidian/
